# Required
AUTH_PASSWORD=changeme

# Path to the SQLite database file.
# In Docker, set this to the volume-mounted path so data survives restarts.
# DATABASE_PATH=/app/data/prompts.db
DATABASE_PATH=./prompts.db
